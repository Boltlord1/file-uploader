<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload</title>
</head>
<body>
    <%- include('navbar') %>
    <main>
        <% if (locals.user) { %>
            <form action=<%- `/upload${path.slice(0, -1)}` %> method="post" enctype="multipart/form-data">
                <input type="file" name="new_file">
                <button type="submit">Submit</button>
            </form>
        <% } else { %>
            <p>You are not logged in.</p>
        <% } %>
    </main>
</body>
</html>