<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index Page</title>
</head>
<body>
    <%- include('navbar') %>
    <main>
        <% if (locals.user) { %>
            <p><%= user.name %></p>
        <% } else { %>
            <p>Not logged in</p>
        <% } %>
    </main>
</body>
</html>